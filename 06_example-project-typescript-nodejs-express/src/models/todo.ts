export class Todo {
  // shorthand to create properties and assign arguments that are passed into the constructor while instantiating of new obj
  constructor(public id: string, public text: string) {
  }
}